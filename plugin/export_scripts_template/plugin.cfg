[plugin]

name="GodotAndroidFilePicker"
description="A Godot Android plugin that allows users to select files from their device with a native file picker dialog. It copies the file to the user folder of the app, so it actually can access it."
author="PostPollux"
version="1.0"
script="export_plugin.gd"
